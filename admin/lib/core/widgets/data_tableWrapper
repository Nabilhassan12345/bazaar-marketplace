import 'package:flutter/material.dart';

class DataTableWrapper extends StatelessWidget {
  const DataTableWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
