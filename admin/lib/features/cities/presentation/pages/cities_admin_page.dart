import 'package:flutter/material.dart';

class CitiesAdminPage extends StatelessWidget {
  const CitiesAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cities')),
      body: const Center(child: Text('Cities')),
    );
  }
}
