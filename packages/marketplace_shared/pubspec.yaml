name: marketplace_shared
description: Shared models, enums, and constants for Bazaar mobile + admin.
publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  cloud_firestore: ^5.6.6

dev_dependencies:
  flutter_lints: ^5.0.0
